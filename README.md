# Weather-App-HW

This is a functional weather application.

It pulls from the openweather api for its data. 

The page will display the current weather, the 5-day forecast, and the UV Index for that specific city/town.

The search history is saved in local storage and the new data is prepeneded to the data display. 

The data is cleared when you press the search button